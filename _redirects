# Proxy API calls to Railway backend - NOTE: Must use proxy (200) not redirect (301/302)
/api/*  https://overflowing-consideration-production.up.railway.app/api/:splat  200!

# SPA catch-all - serve index.html for client-side routing
/*  /index.html  200
